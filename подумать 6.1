#include <iostream>
#include <vector>
using namespace std;
int main() {
    int st;
    cout<<"How many students are there? : ";
    cin >> st;
    vector<int> students(st);
    for (int i = 0; i < st; i++) {
        cout<<"Student numb "<<i+1<<" wants to leave a couple of... ";
        cin >> students[i]; //ввод студентов
    }
    vector<int> result;
    int st_removed = 0;
        for (int i = 0; i < students.size(); i++) {
            if (!result.empty() && students[i] == students[i + 1]) {
                st_removed=st_removed+2;
                i++;
                result.pop_back();
            } else {
                result.push_back(students[i]);
            }
        }   
    students = result;
    
    cout << st_removed;
    
}
