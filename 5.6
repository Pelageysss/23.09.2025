#include <iostream>
using namespace std;
int main() 
{
    cout << " Choose size of array. "<<"\n";
    int size_arr_n;
    int size_arr_m;
    int sum=0;
    cout<<"lines and columns: ";
    cin >> size_arr_n;
    size_arr_m = size_arr_n;
    int array[size_arr_n][size_arr_m];
    for (int i = 0; i < size_arr_n; i++)
    {
        sum =sum+ array[i][i];
    }
    cout<<"summ : "<<sum;
} 
